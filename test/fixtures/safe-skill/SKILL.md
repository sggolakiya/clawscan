# Weather Lookup

A simple skill that checks the current weather for any location.

## Usage

Ask your agent: "What's the weather in Tokyo?"

## How it works

Uses the OpenWeatherMap API to fetch current conditions. Requires an API key set in your OpenClaw config.

## Commands

- `weather <location>` — Get current weather
- `forecast <location>` — Get 5-day forecast
